/* ********************************************************** *
 * AMNS NitroCast v1.0 Class Collection Object Business Tier    *
 * Autogenerated by NitroCast © 2007 Roy A.E Hodges             *
 * All Rights Reserved                                        *
 * ---------------------------------------------------------- *
 * Source code may not be reproduced or redistributed without *
 * written expressed permission from the author.              *
 * Permission is granted to modify source code by licencee.   *
 * These permissions do not extend to third parties.          *
 * ********************************************************** */

using System;
using System.Collections;

namespace Amns.Tessen
{
	/// <summary>
	/// <summary>
	/// Promotion Status
	/// </summary>
	/// </summary>
	public class DojoPromotionStatusCollection : IList, ICloneable 
	{
		private int count = 0;
		private DojoPromotionStatus[] DojoPromotionStatusArray ;

		public DojoPromotionStatusCollection() : base()
		{
			DojoPromotionStatusArray = new DojoPromotionStatus[15];
		}

		public DojoPromotionStatusCollection(int capacity) : base()
		{
			DojoPromotionStatusArray = new DojoPromotionStatus[capacity];
		}

		#region IList Implemenation

		public bool IsFixedSize
		{
			get
			{
				return false;
			}
		}

		public bool IsReadOnly
		{
			get
			{
				return false;
			}
		}

		object IList.this[int index]
		{
			get
			{
				return this[index];
			}
			set
			{
				DojoPromotionStatusArray[index] = (DojoPromotionStatus) value;
			}
		}

		public DojoPromotionStatus this[int index]
		{
			get
			{
				lock(this)
				{
					if(index > count - 1)
						throw(new Exception("Index out of bounds."));
					return DojoPromotionStatusArray[index];
				}
			}
			set
			{
				OnCollectionChanged(EventArgs.Empty);
				DojoPromotionStatusArray[index] = value;
			}
		}

		int IList.Add(object value)
		{
			return Add((DojoPromotionStatus) value);
		}

		public int Add(DojoPromotionStatus value)
		{
			OnCollectionChanged(EventArgs.Empty);
			lock(this)
			{
				count++;
				// Resize the array if the count is greater than the length 
				// of the array.
				if(count > DojoPromotionStatusArray.GetUpperBound(0) + 1)
				{
					DojoPromotionStatus[] tempDojoPromotionStatusArray = new DojoPromotionStatus[count * 2];
					Array.Copy(DojoPromotionStatusArray, tempDojoPromotionStatusArray, count - 1);
					DojoPromotionStatusArray = tempDojoPromotionStatusArray;
				}
				DojoPromotionStatusArray[count - 1] = value;
			}
			return count -1;
		}

		public void Clear()
		{
			OnCollectionChanged(EventArgs.Empty);
			lock(this)
			{
				count = 0;
				DojoPromotionStatusArray = new DojoPromotionStatus[15];
			}
		}

		bool IList.Contains(object value)
		{
			return Contains((DojoPromotionStatus) value);
		}

		public bool Contains(DojoPromotionStatus value)
		{
			return IndexOf(value) != -1;
		}

		int IList.IndexOf(object value)
		{
			return IndexOf((DojoPromotionStatus) value);
		}

		public int IndexOf(DojoPromotionStatus value)
		{
			lock(this)
			{
				for(int x = 0; x < count; x++)
					if(DojoPromotionStatusArray[x].Equals(value))
						return x;
				return -1;
			}
		}

		void IList.Insert(int index, object value)
		{
			Insert(index, (DojoPromotionStatus) value);
		}

		public void Insert(int index, DojoPromotionStatus value)
		{
			OnCollectionChanged(EventArgs.Empty);
			lock(this)
			{
				count++;
				// Resize the array if the count is greater than the length 
				// of the array.
				if(count > DojoPromotionStatusArray.GetUpperBound(0) + 1)
				{
					DojoPromotionStatus[] tempDojoPromotionStatusArray = new DojoPromotionStatus[count * 2];
					Array.Copy(DojoPromotionStatusArray, tempDojoPromotionStatusArray, count - 1);
					DojoPromotionStatusArray = tempDojoPromotionStatusArray;
				}
				for(int x = index + 1; x == count - 2; x ++)
					DojoPromotionStatusArray[x] = DojoPromotionStatusArray[x - 1];
				DojoPromotionStatusArray[index] = value;
			}
		}

		void IList.Remove(object value)
		{
			Remove((DojoPromotionStatus) value);
		}

		public void Remove(DojoPromotionStatus value)
		{
			OnCollectionChanged(EventArgs.Empty);
			int index = IndexOf(value);
			if(index == -1)
				throw(new Exception("DojoPromotionStatus not found in collection."));
			RemoveAt(index);
		}

		public void RemoveAt(int index)
		{
			OnCollectionChanged(EventArgs.Empty);
			lock(this)
			{
				for(int x = index + 1; x <= count - 1; x++)
					DojoPromotionStatusArray[x-1] = DojoPromotionStatusArray[x];
				DojoPromotionStatusArray[count - 1] = null;
				count--;
			}
		}

		#endregion

		#region ICollection Implementation

		public int Count
		{
			get
			{
				return count;
			}
		}

		public bool IsSynchronized
		{
			get
			{
				return DojoPromotionStatusArray.IsSynchronized;
			}
		}

		public object SyncRoot
		{
			get
			{
				return DojoPromotionStatusArray;
			}
		}

		public void CopyTo(Array array, int index)
		{
			lock(this)
			{
				DojoPromotionStatusArray.CopyTo(array, index);
			}
		}

		#endregion

		#region IEnumerator Implementation

		public Enumerator GetEnumerator()
		{
			return new Enumerator(DojoPromotionStatusArray, count);
		}

		IEnumerator IEnumerable.GetEnumerator()
		{
			return GetEnumerator();
		}

		public class Enumerator : IEnumerator
		{
			private DojoPromotionStatus[] DojoPromotionStatusArray;
			private int cursor;
			private int virtualCount;

			public Enumerator(DojoPromotionStatus[] DojoPromotionStatusArray, int virtualCount)
			{
				this.DojoPromotionStatusArray = DojoPromotionStatusArray;
				this.virtualCount = virtualCount;
				cursor = -1;
			}

			public void Reset()
			{
				cursor = -1;
			}

			public bool MoveNext()
			{
				if(cursor < DojoPromotionStatusArray.Length)
					cursor++;
				return(!(cursor == virtualCount));
			}

			public DojoPromotionStatus Current
			{
				get
				{
					if((cursor < 0) || (cursor == virtualCount))
						throw(new InvalidOperationException());
					return DojoPromotionStatusArray[cursor];
				}
			}

			Object IEnumerator.Current
			{
				get
				{
					return Current;
				}
			}
		}

		#endregion

		/// <summary>
		/// Makes a shallow copy of the current DojoPromotionStatusCollection.
		/// as the parent object.
		/// </summary>
		/// <returns>DojoPromotionStatusCollection</returns>
		#region ICloneable Implementation

		object ICloneable.Clone()
		{
			return Clone();
		}

		/// <summary>
		/// Makes a shallow copy of the current DojoPromotionStatusCollection.
		/// as the parent object.
		/// </summary>
		/// <returns>DojoPromotionStatusCollection</returns>
		public DojoPromotionStatusCollection Clone()
		{
			DojoPromotionStatusCollection clonedDojoPromotionStatus = new DojoPromotionStatusCollection(count);
			lock(this)
			{
				foreach(DojoPromotionStatus item in this)
					clonedDojoPromotionStatus.Add(item);
			}
			return clonedDojoPromotionStatus;
		}

		/// <summary>
		/// Makes a deep copy of the current DojoPromotionStatus.
		/// </summary>
		/// <param name="isolation">Placeholders are used to isolate the 
		/// items in the DojoPromotionStatusCollection from their children.</param>
		public DojoPromotionStatusCollection Copy(bool isolated)
		{
			DojoPromotionStatusCollection isolatedCollection = new DojoPromotionStatusCollection(count);

			lock(this)
			{
				if(isolated)
				{
					for(int i = 0; i < count; i++)
					{
						isolatedCollection.Add(DojoPromotionStatusArray[i].NewPlaceHolder());
					}
				}
				else
				{
					for(int i = 0; i < count; i++)
					{
						isolatedCollection.Add(DojoPromotionStatusArray[i].Copy());
					}
				}
			}
			return isolatedCollection;
		}

		#endregion

		#region Events

		public event EventHandler CollectionChanged;

		protected virtual void OnCollectionChanged(EventArgs e)
		{
			if(CollectionChanged != null)
				CollectionChanged(this, e);
		}

		#endregion

		#region Sort Methods

		/// <summary>
		/// Sorts the collection by id.
		/// </summary>
		public void Sort()
		{
			lock(this)
			{
				Array.Sort(DojoPromotionStatusArray, 0, count);
			}
		}

		#endregion

		#region Find Methods

		/// <summary>
		/// Finds a record by ID.
		/// </summary>
		public DojoPromotionStatus Find(int id)
		{
			lock(this)
			{
				for(int x = 0; x < count; x++)
					if(DojoPromotionStatusArray[x].ID == id)
						return DojoPromotionStatusArray[x];
			}
			return null;
		}

		#endregion

		#region ToString() Override Method

		public override string ToString()
		{
			string lineBreak;

			if(System.Web.HttpContext.Current != null)
			lineBreak = "<br />";
			else
			lineBreak = "\r\n";

			System.Text.StringBuilder s = new System.Text.StringBuilder();
			for(int x = 0; x < count; x++)
			{
				if(x != 0)
					s.Append(lineBreak);
				s.Append(DojoPromotionStatusArray[x].ToString());
			}

			return s.ToString();
		}

		#endregion

	}
}
