/* ********************************************************** *
 * AMNS NitroCast v1.0 Class Collection Object Business Tier    *
 * Autogenerated by NitroCast © 2007 Roy A.E Hodges             *
 * All Rights Reserved                                        *
 * ---------------------------------------------------------- *
 * Source code may not be reproduced or redistributed without *
 * written expressed permission from the author.              *
 * Permission is granted to modify source code by licencee.   *
 * These permissions do not extend to third parties.          *
 * ********************************************************** */

using System;
using System.Collections;

namespace Amns.Tessen
{
	/// <summary>
	/// <summary>
	/// A list for canidates of a dojo test.
	/// </summary>
	/// </summary>
	public class DojoTestListCollection : IList, ICloneable 
	{
		private int count = 0;
		private DojoTestList[] DojoTestListArray ;

		public DojoTestListCollection() : base()
		{
			DojoTestListArray = new DojoTestList[15];
		}

		public DojoTestListCollection(int capacity) : base()
		{
			DojoTestListArray = new DojoTestList[capacity];
		}

		#region IList Implemenation

		public bool IsFixedSize
		{
			get
			{
				return false;
			}
		}

		public bool IsReadOnly
		{
			get
			{
				return false;
			}
		}

		object IList.this[int index]
		{
			get
			{
				return this[index];
			}
			set
			{
				DojoTestListArray[index] = (DojoTestList) value;
			}
		}

		public DojoTestList this[int index]
		{
			get
			{
				lock(this)
				{
					if(index > count - 1)
						throw(new Exception("Index out of bounds."));
					return DojoTestListArray[index];
				}
			}
			set
			{
				OnCollectionChanged(EventArgs.Empty);
				DojoTestListArray[index] = value;
			}
		}

		int IList.Add(object value)
		{
			return Add((DojoTestList) value);
		}

		public int Add(DojoTestList value)
		{
			OnCollectionChanged(EventArgs.Empty);
			lock(this)
			{
				count++;
				// Resize the array if the count is greater than the length 
				// of the array.
				if(count > DojoTestListArray.GetUpperBound(0) + 1)
				{
					DojoTestList[] tempDojoTestListArray = new DojoTestList[count * 2];
					Array.Copy(DojoTestListArray, tempDojoTestListArray, count - 1);
					DojoTestListArray = tempDojoTestListArray;
				}
				DojoTestListArray[count - 1] = value;
			}
			return count -1;
		}

		public void Clear()
		{
			OnCollectionChanged(EventArgs.Empty);
			lock(this)
			{
				count = 0;
				DojoTestListArray = new DojoTestList[15];
			}
		}

		bool IList.Contains(object value)
		{
			return Contains((DojoTestList) value);
		}

		public bool Contains(DojoTestList value)
		{
			return IndexOf(value) != -1;
		}

		int IList.IndexOf(object value)
		{
			return IndexOf((DojoTestList) value);
		}

		public int IndexOf(DojoTestList value)
		{
			lock(this)
			{
				for(int x = 0; x < count; x++)
					if(DojoTestListArray[x].Equals(value))
						return x;
				return -1;
			}
		}

		void IList.Insert(int index, object value)
		{
			Insert(index, (DojoTestList) value);
		}

		public void Insert(int index, DojoTestList value)
		{
			OnCollectionChanged(EventArgs.Empty);
			lock(this)
			{
				count++;
				// Resize the array if the count is greater than the length 
				// of the array.
				if(count > DojoTestListArray.GetUpperBound(0) + 1)
				{
					DojoTestList[] tempDojoTestListArray = new DojoTestList[count * 2];
					Array.Copy(DojoTestListArray, tempDojoTestListArray, count - 1);
					DojoTestListArray = tempDojoTestListArray;
				}
				for(int x = index + 1; x == count - 2; x ++)
					DojoTestListArray[x] = DojoTestListArray[x - 1];
				DojoTestListArray[index] = value;
			}
		}

		void IList.Remove(object value)
		{
			Remove((DojoTestList) value);
		}

		public void Remove(DojoTestList value)
		{
			OnCollectionChanged(EventArgs.Empty);
			int index = IndexOf(value);
			if(index == -1)
				throw(new Exception("DojoTestList not found in collection."));
			RemoveAt(index);
		}

		public void RemoveAt(int index)
		{
			OnCollectionChanged(EventArgs.Empty);
			lock(this)
			{
				for(int x = index + 1; x <= count - 1; x++)
					DojoTestListArray[x-1] = DojoTestListArray[x];
				DojoTestListArray[count - 1] = null;
				count--;
			}
		}

		#endregion

		#region ICollection Implementation

		public int Count
		{
			get
			{
				return count;
			}
		}

		public bool IsSynchronized
		{
			get
			{
				return DojoTestListArray.IsSynchronized;
			}
		}

		public object SyncRoot
		{
			get
			{
				return DojoTestListArray;
			}
		}

		public void CopyTo(Array array, int index)
		{
			lock(this)
			{
				DojoTestListArray.CopyTo(array, index);
			}
		}

		#endregion

		#region IEnumerator Implementation

		public Enumerator GetEnumerator()
		{
			return new Enumerator(DojoTestListArray, count);
		}

		IEnumerator IEnumerable.GetEnumerator()
		{
			return GetEnumerator();
		}

		public class Enumerator : IEnumerator
		{
			private DojoTestList[] DojoTestListArray;
			private int cursor;
			private int virtualCount;

			public Enumerator(DojoTestList[] DojoTestListArray, int virtualCount)
			{
				this.DojoTestListArray = DojoTestListArray;
				this.virtualCount = virtualCount;
				cursor = -1;
			}

			public void Reset()
			{
				cursor = -1;
			}

			public bool MoveNext()
			{
				if(cursor < DojoTestListArray.Length)
					cursor++;
				return(!(cursor == virtualCount));
			}

			public DojoTestList Current
			{
				get
				{
					if((cursor < 0) || (cursor == virtualCount))
						throw(new InvalidOperationException());
					return DojoTestListArray[cursor];
				}
			}

			Object IEnumerator.Current
			{
				get
				{
					return Current;
				}
			}
		}

		#endregion

		/// <summary>
		/// Makes a shallow copy of the current DojoTestListCollection.
		/// as the parent object.
		/// </summary>
		/// <returns>DojoTestListCollection</returns>
		#region ICloneable Implementation

		object ICloneable.Clone()
		{
			return Clone();
		}

		/// <summary>
		/// Makes a shallow copy of the current DojoTestListCollection.
		/// as the parent object.
		/// </summary>
		/// <returns>DojoTestListCollection</returns>
		public DojoTestListCollection Clone()
		{
			DojoTestListCollection clonedDojoTestList = new DojoTestListCollection(count);
			lock(this)
			{
				foreach(DojoTestList item in this)
					clonedDojoTestList.Add(item);
			}
			return clonedDojoTestList;
		}

		/// <summary>
		/// Makes a deep copy of the current DojoTestList.
		/// </summary>
		/// <param name="isolation">Placeholders are used to isolate the 
		/// items in the DojoTestListCollection from their children.</param>
		public DojoTestListCollection Copy(bool isolated)
		{
			DojoTestListCollection isolatedCollection = new DojoTestListCollection(count);

			lock(this)
			{
				if(isolated)
				{
					for(int i = 0; i < count; i++)
					{
						isolatedCollection.Add(DojoTestListArray[i].NewPlaceHolder());
					}
				}
				else
				{
					for(int i = 0; i < count; i++)
					{
						isolatedCollection.Add(DojoTestListArray[i].Copy());
					}
				}
			}
			return isolatedCollection;
		}

		#endregion

		#region Events

		public event EventHandler CollectionChanged;

		protected virtual void OnCollectionChanged(EventArgs e)
		{
			if(CollectionChanged != null)
				CollectionChanged(this, e);
		}

		#endregion

		#region Sort Methods

		/// <summary>
		/// Sorts the collection by id.
		/// </summary>
		public void Sort()
		{
			lock(this)
			{
				Array.Sort(DojoTestListArray, 0, count);
			}
		}

		#endregion

		#region Find Methods

		/// <summary>
		/// Finds a record by ID.
		/// </summary>
		public DojoTestList Find(int id)
		{
			lock(this)
			{
				for(int x = 0; x < count; x++)
					if(DojoTestListArray[x].ID == id)
						return DojoTestListArray[x];
			}
			return null;
		}

		#endregion

		#region ToString() Override Method

		public override string ToString()
		{
			string lineBreak;

			if(System.Web.HttpContext.Current != null)
			lineBreak = "<br />";
			else
			lineBreak = "\r\n";

			System.Text.StringBuilder s = new System.Text.StringBuilder();
			for(int x = 0; x < count; x++)
			{
				if(x != 0)
					s.Append(lineBreak);
				s.Append(DojoTestListArray[x].ToString());
			}

			return s.ToString();
		}

		#endregion

	}
}
